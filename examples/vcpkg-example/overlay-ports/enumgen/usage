This package `enumgen` provides the CMake package:

    find_package(enumgen CONFIG REQUIRED)

The package auto-includes the `enumgen` functions, which can be used to generate enums at configure time

    enumgen_generate(<INPUT_FILE> <CONFIG_FILE>)

For more information, see the docs here:

    https://github.com/JonathanHiggs/enumgen
